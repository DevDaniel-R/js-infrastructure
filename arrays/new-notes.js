const notes = ['Notes 0', 'Notes 1', 'Notes 2']

// notes[2] = 'this is now the new note 3 index 2'

// notes.forEach(function (index, item){
//   console.log(item)
//   console.log(index)
// })

// console.log(notes.length)
// console.log(notes)

//Counting...

for (let count = 0; count <= 2; count++) {
console.log(count)
}

for (let count = notes.length - 1; count >= 0; count--){
  console.log(notes[count])
}